#!/usr/bin/env bash

#
# Usage: serve [port] [dir]
#

set -euo pipefail

port="${1:-8000}"
dir="${2:-development}"

root="${build:?}/$dir"

caddy file-server --browse --listen :"$port" --root "$root"
